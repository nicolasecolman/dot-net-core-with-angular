import { DisableButtonDuringAjax.Directive.TsDirective } from './disable-button-during-ajax.directive';

describe('DisableButtonDuringAjax.Directive.TsDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableButtonDuringAjax.Directive.TsDirective();
    expect(directive).toBeTruthy();
  });
});
